import { describe, it } from "vitest";

describe("(app)/budgets/new/+page.server actions.create", () => {
  it.skip("redirige al preview del presupuesto creado", async () => {
    // TODO: Fix mock - saving to budgets/save module doesn't work with SvelteKit ES modules
  });

  it.skip("devuelve failure cuando saveBudget falla validacion", async () => {
    // TODO: Fix mock - saving to budgets/save module doesn't work with SvelteKit ES modules
  });
});
